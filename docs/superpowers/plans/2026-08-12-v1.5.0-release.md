# v1.5.0 릴리스 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 이슈 #90(mysql·oracle 뷰에서 사라지는 실습 예제)을 닫고, 같은 부류를 다시 들이지 않을 검사를 붙인 뒤, `v1.5.0`을 발행한다.

**Architecture:** `check_content.check_structure`에 `dbms` 인자를 더해 `filter_dbms.filter_lines`(벤더 브랜치를 만드는 바로 그 함수)를 통과시킨 뒤 검사하게 만든다. 그 검사를 `ShippedContentTest`에 세 벤더로 걸어 두면 CI가 `main`에서 잡는다. 그 다음 `02-intermediate/04-backup-recovery-strategies.md`의 실습 예제를 공통 시나리오 + 벤더별 완전 절차로 재배치해 검사를 초록으로 만든다.

**Tech Stack:** Python 3 표준 라이브러리만 (`argparse`, `re`, `pathlib`, `unittest`) + Markdown 본문.

**설계 문서:** [`docs/superpowers/specs/2026-08-12-v1.5.0-release-design.md`](../specs/2026-08-12-v1.5.0-release-design.md) — 각 결정의 근거는 거기 있다.

**브랜치:** `Ahngbeom/issue-90` (설계 문서 커밋 `4e5eba8`이 이미 올라가 있다). `main`은 `9634706`.

## Global Constraints

- **Python 표준 라이브러리만.** `pip`/`npm`/빌드 시스템이 없고 CI도 PyPI에서 아무것도 설치하지 않는다.
- **Python 3.9에서 동작해야 한다** — 3.10+ 문법 금지 (CI가 ubuntu × 3.9/3.13, macOS × bash 3.2로 돈다).
- **모든 새 주석·docstring·본문·화면 문구는 한국어.**
- **전체 스위트 명령은 하나뿐이다:** `python3 -m unittest discover -s tests` (저장소 루트에서). **시작 기준선(실측): 881개 통과, 약 101초.** 개별 Step에서 쓰는 `python3 -m unittest tests.test_check_content.<클래스>` 형태의 표적 실행은 반복이 잦은 자리를 빠르게 돌기 위한 것이고 (`tests/`에 `__init__.py`가 없어도 동작하는 것을 확인했다), **각 Task는 반드시 전체 스위트로 닫는다.**
- **검사기 실행:** `python3 scripts/check_content.py` — exit 0이어야 한다.
- **`scripts/filter_dbms.py`는 고치지 않는다.** 이 계획은 `filter_lines`를 **호출만** 한다. 그 함수는 `generate-branch.sh`가 벤더 브랜치를 만들 때 쓰는 바로 그 코드이고, 검사기를 위해 손대면 검사와 브랜치가 갈라진다.
- **`tests/test_check_content.py`의 `_skip_on_a_vendor_branch`(317행)는 그대로 둔다.** 필터된 뷰를 또 필터하는 것은 무의미하고, CI 트리거가 `push: branches: [main]`이라 벤더 브랜치에서는 어차피 돌지 않는다.
- **벤더 마커는 `<!-- dbms:X -->` / `<!-- /dbms:X -->`로 정확히 쓴다.** `filter_lines`의 `OPEN_RE`/`CLOSE_RE`가 `^\s*<!--\s*dbms:(\w+)\s*-->\s*$`로 줄 전체를 잡으므로, 마커는 **자기 줄을 독차지해야 한다.**
- **치트시트 표는 세 칸을 모두 채운다.** 현행 77개 행 전부가 그렇고 "해당 없음" 표기 관행이 없다. 한 벤더에 대응물이 없는 개념은 표가 아니라 용어집에 넣는다.
- 커밋 메시지는 영어 명령형 한 줄 + 본문. 끝에 `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.

## 파일 구조

| 파일 | 이 계획에서의 책임 | Task |
|---|---|---|
| `scripts/check_content.py` | `check_structure(root, dbms=None)` + `--dbms` 플래그. 필터는 `filter_dbms`에 위임한다 | 1 |
| `tests/test_check_content.py` | 픽스처 기반 벤더 검사 6개 + CLI 2개 + `ShippedContentTest` 벤더 가드 | 1, 2 |
| `02-intermediate/04-backup-recovery-strategies.md` | #90 본문 — 실습 예제 재배치, 체크리스트 벤더 쌍 | 2 |
| `02-intermediate/09-commands-cheatsheet.md` | 「백업 / 복구」 표에 `연속 로그 목록 확인` 행 | 3 |
| `appendix/glossary.md` | `RESETLOGS`, `불완전 복구` 두 항목 | 3 |
| `docs/release-policy.md` | 「릴리스 전 점검」에 `--dbms` 항목 | 3 |
| `CLAUDE.md` | `check_content.py` 설명에 벤더 필터 검사 반영 | 3 |
| `docs/superpowers/notes/2026-08-12-v1.5.0-release-notes.md` | 릴리스 노트 초안 (본문에 넣지 않고 `gh release`에 `--notes-file`로 넘긴다) | 4 |

---

## Task 1: 검사기에 벤더 필터

**Files:**
- Modify: `scripts/check_content.py:20-25` (import), `:138-156` (`check_structure`), `:207-227` (`main`)
- Test: `tests/test_check_content.py` — `CHAPTER` 상수 아래에 `VENDOR_CHAPTER` 추가, `StructureTest`(183-240행) 뒤에 새 클래스 2개

**Interfaces:**
- Consumes: `filter_dbms.filter_lines(lines, target) -> list[str]` — `scripts/filter_dbms.py:17`. 균형이 맞지 않는 마커나 닫히지 않은 코드 펜스에 `ValueError`를 던진다.
- Produces:
  - `check_content.check_structure(root, dbms=None) -> list[str]` — `dbms`가 `None`이면 현행과 완전히 동일하게 동작한다(기존 호출부 `CHECKS`가 그대로 쓴다).
  - `check_content.main(argv)`가 `--dbms {postgresql,mysql,oracle}`를 받는다. 주면 **structure만** 그 벤더 뷰로 검사하고, 라벨은 `structure:<dbms>`다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_check_content.py`의 `CHAPTER` 상수(27-45행) **바로 아래**에 픽스처를 추가한다.

```python
# 절 하나가 통째로 한 벤더 마커 안에 들어간 챕터. `main`에서 읽으면 네 절이
# 다 보이므로 정상으로 보이고, 다른 두 벤더의 뷰에서만 절이 사라진다 —
# 이슈 #90이 실제로 그 모양이었다.
VENDOR_CHAPTER = """\
# 1장

## 핵심 개념 설명

내용.

## 주요 명령어/문법

내용.

<!-- dbms:postgresql -->
## 실습 예제 — PostgreSQL PITR

내용.
<!-- /dbms:postgresql -->

## 체크리스트

- [ ] 할 수 있다
"""
```

`StructureTest`(183-240행) **뒤에** 두 클래스를 추가한다.

```python
class VendorStructureTest(FixtureTestCase):
    """단일 벤더 뷰에서만 드러나는 구조 결함 (#90)."""

    def test_unfiltered_view_looks_fine(self):
        """`main`에서만 보면 정상이다 — 이것이 이 결함이 숨는 방식이다."""
        self.chapter.write_text(VENDOR_CHAPTER, encoding="utf-8")
        self.assertEqual(check_content.check_structure(self.root), [])

    def test_the_section_survives_in_its_own_vendor_view(self):
        self.chapter.write_text(VENDOR_CHAPTER, encoding="utf-8")
        self.assertEqual(
            check_content.check_structure(self.root, "postgresql"), [])

    def test_the_section_vanishes_from_the_other_vendor_views(self):
        self.chapter.write_text(VENDOR_CHAPTER, encoding="utf-8")
        for dbms in ("mysql", "oracle"):
            with self.subTest(dbms=dbms):
                found = check_content.check_structure(self.root, dbms)
                self.assertEqual(len(found), 1, found)
                self.assertIn("실습 예제", found[0])
                self.assertIn("01-intro.md", found[0])

    def test_a_chapter_without_markers_reads_the_same_in_every_view(self):
        """마커가 없는 파일은 어느 뷰에서도 바이트 단위로 같다."""
        for dbms in (None, "postgresql", "mysql", "oracle"):
            with self.subTest(dbms=dbms):
                self.assertEqual(
                    check_content.check_structure(self.root, dbms), [])

    def test_an_unbalanced_marker_is_reported_not_raised(self):
        """`filter_lines`의 ValueError가 트레이스백으로 새어 나가면 안 된다.

        검사기는 어디가 틀렸는지 한 줄씩 찍어 주는 도구다. 마커 하나가
        안 닫혔다고 전체 검사가 중단되면 나머지 위반을 못 본다.
        """
        self.chapter.write_text(
            VENDOR_CHAPTER.replace("<!-- /dbms:postgresql -->\n", ""),
            encoding="utf-8")
        found = check_content.check_structure(self.root, "mysql")
        self.assertEqual(len(found), 1, found)
        self.assertIn("01-intro.md", found[0])

    def test_markers_inside_a_code_fence_are_not_directives(self):
        """마커 문법을 설명하는 본문이 자기 자신에게 걸리지 않아야 한다."""
        self.chapter.write_text(
            CHAPTER + "\n```\n<!-- dbms:oracle -->\n```\n", encoding="utf-8")
        self.assertEqual(
            check_content.check_structure(self.root, "mysql"), [])


class VendorCliTest(FixtureTestCase):
    """`--dbms`는 릴리스 전에 사람이 직접 돌리는 통로다."""

    def run_main(self, *extra):
        buf = io.StringIO()
        with contextlib.redirect_stdout(buf):
            rc = check_content.main(["--root", str(self.root), *extra])
        return rc, buf.getvalue()

    def test_the_flag_reports_only_the_filtered_structure_check(self):
        self.chapter.write_text(VENDOR_CHAPTER, encoding="utf-8")
        rc, out = self.run_main("--dbms", "mysql")
        self.assertEqual(rc, 1)
        self.assertIn("structure:mysql", out)
        self.assertIn("실습 예제", out)
        self.assertNotIn("[links]", out)

    def test_the_flag_passes_for_the_matching_vendor(self):
        self.chapter.write_text(VENDOR_CHAPTER, encoding="utf-8")
        rc, out = self.run_main("--dbms", "postgresql")
        self.assertEqual(rc, 0, out)
        self.assertIn("structure:postgresql", out)

    def test_without_the_flag_all_four_checks_still_run(self):
        rc, out = self.run_main()
        self.assertEqual(rc, 0, out)
        for name in ("[links]", "[orphans]", "[structure]", "[banks]"):
            self.assertIn(name, out)
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python3 -m unittest tests.test_check_content.VendorStructureTest tests.test_check_content.VendorCliTest -v`

Expected: FAIL. `check_structure() takes 1 positional argument but 2 were given` (VendorStructureTest 5개), `unrecognized arguments: --dbms` 로 인한 `SystemExit: 2` (VendorCliTest 2개). `test_unfiltered_view_looks_fine`과 `test_without_the_flag_all_four_checks_still_run`은 처음부터 통과한다 — 기존 동작을 건드리지 않았다는 기준선이다.

- [ ] **Step 3: `check_structure`에 `dbms`를 더한다**

`scripts/check_content.py`의 import 구역(20-25행)에 한 줄 추가한다. `check_content.py`와 `filter_dbms.py`는 같은 `scripts/`에 있고, 직접 실행하면 `sys.path[0]`이 `scripts/`, 테스트는 21행에서 `scripts/`를 넣으므로 양쪽에서 풀린다.

```python
import filter_dbms
```

`check_structure`(138-156행)를 통째로 교체한다.

```python
def check_structure(root, dbms=None):
    """챕터가 네 절을 정해진 순서로 갖는가.

    `dbms`를 주면 그 벤더로 필터한 뒤 검사한다 — 단일 벤더 브랜치에서
    실제로 보이는 모습이다. 절 하나가 통째로 다른 벤더의 마커 안에 있으면
    그 브랜치에서만 사라지므로, `main`에서만 보면 드러나지 않는다(#90).

    필터는 `filter_dbms.filter_lines`를 그대로 쓴다 —
    `scripts/generate-branch.sh`가 벤더 브랜치를 만들 때, `reading.py`가
    화면에 뿌릴 때 부르는 바로 그 함수다. 여기서 따로 구현하면 "검사는
    통과했는데 브랜치는 깨진" 상태가 생길 수 있다.
    """
    problems = []
    for p in chapters(root):
        lines = p.read_text(encoding="utf-8").splitlines()
        if dbms is not None:
            try:
                lines = filter_dbms.filter_lines(lines, dbms)
            except ValueError as e:
                problems.append(f"{p.relative_to(root)}: {e}")
                continue
        heads = [m.group(1) for m in
                 (HEADING_RE.match(line) for line in lines) if m]
        at = -1
        for names in SECTION_ORDER:
            for i, head in enumerate(heads):
                if i > at and any(head.startswith(n) for n in names):
                    at = i
                    break
            else:
                problems.append(
                    f"{p.relative_to(root)}: `## {names[0]}` 절이 없거나 "
                    f"순서가 어긋난다")
    return problems
```

바뀐 곳은 셋뿐이다 — 시그니처, `lines`를 먼저 뽑아 필터를 태우는 부분, 그리고 `heads` 목록이 `p.read_text(...)` 대신 `lines`를 읽는 것. `at`을 다루는 순서 판정 로직은 한 글자도 바뀌지 않는다.

- [ ] **Step 4: `main`에 `--dbms`를 더한다**

`scripts/check_content.py:207-221`. `ap.add_argument("--root", ...)` 아래에 한 줄, 그리고 출력 루프가 도는 대상을 갈라 준다.

```python
def main(argv=None):
    ap = argparse.ArgumentParser(description=__doc__.splitlines()[0])
    ap.add_argument("--root", default=str(REPO_ROOT),
                    help="검사할 저장소 경로 (기본: 이 스크립트의 저장소)")
    ap.add_argument("--dbms", choices=("postgresql", "mysql", "oracle"),
                    help="단일 벤더 뷰로 필터한 뒤 structure만 검사한다")
    args = ap.parse_args(argv)

    if args.dbms:
        checks = [(f"structure:{args.dbms}",
                   check_structure(Path(args.root), args.dbms))]
    else:
        checks = check_all(args.root)

    total = 0
    for name, problems in checks:
        if problems:
            total += len(problems)
            print(f"\n[{name}] {len(problems)}건")
            for line in problems:
                print(f"  {line}")
        else:
            print(f"[{name}] ok")

    if total:
        print(f"\n{total}건. 위 항목을 고치세요.")
        return 1
    print("\n모두 통과.")
    return 0
```

`--dbms`가 links·orphans·banks를 건너뛰는 것은 의도다. 필터링은 파일을 지우지 않으므로 그 셋의 결과가 벤더별로 달라질 이유가 없고, 셋을 같이 찍으면 "이 벤더에서 링크가 검사됐다"는 잘못된 인상을 준다.

모듈 docstring(9-19행)의 사용법 블록에도 한 줄 더한다.

```
사용:
    python3 scripts/check_content.py          # 위반이 있으면 종료 코드 1
    python3 scripts/check_content.py --root DIR
    python3 scripts/check_content.py --dbms mysql   # 단일 벤더 뷰의 structure만
```

- [ ] **Step 5: 테스트를 다시 돌려 통과를 확인한다**

Run: `python3 -m unittest tests.test_check_content -v`

Expected: PASS. 새 테스트 9개 포함 전부 초록.

- [ ] **Step 6: 전체 스위트로 회귀를 확인한다**

Run: `python3 -m unittest discover -s tests`

Expected: OK, 890개 (881 + 9). `check_structure`의 기본 동작을 건드리지 않았으므로 기존 `StructureTest`·`ShippedContentTest`가 그대로 통과해야 한다.

- [ ] **Step 7: 저장소 본문에 대고 실제로 돌려 본다 — 이 시점에서 mysql·oracle은 빨개야 한다**

Run:
```bash
python3 scripts/check_content.py
python3 scripts/check_content.py --dbms postgresql
python3 scripts/check_content.py --dbms mysql
python3 scripts/check_content.py --dbms oracle
```

Expected (실측된 값 그대로):
- 인자 없음 → `모두 통과.`, exit 0
- `--dbms postgresql` → `[structure:postgresql] ok`, exit 0
- `--dbms mysql` → `[structure:mysql] 1건` / `02-intermediate/04-backup-recovery-strategies.md: \`## 실습 예제\` 절이 없거나 순서가 어긋난다`, exit 1
- `--dbms oracle` → 같은 내용, exit 1

**이 빨간색이 Task 2의 출발점이다.** 여기서 초록이 나오면 검사기가 아무것도 보고 있지 않다는 뜻이므로 Step 3으로 돌아간다.

- [ ] **Step 8: 커밋**

```bash
git add scripts/check_content.py tests/test_check_content.py
git commit -F - <<'MSG'
Let the structure check look through a single vendor's eyes

check_structure read the tree as main has it, which is the one view where
a section wholly inside a <!-- dbms:x --> block still looks present. It
gains a dbms argument that runs the lines through filter_dbms.filter_lines
first — the same function generate-branch.sh and reading.py call, so the
check cannot drift from the branches it stands for.

Pointed at the shipped tree it reports postgresql 0, mysql 1, oracle 1,
all the same file: issue #90. The fix for that lands next; this commit
only makes the defect visible to the suite.

Unbalanced markers and unclosed fences come back as reported problems
rather than a ValueError, because a checker that stops at the first bad
marker hides every violation after it.

--dbms runs structure alone. Filtering removes lines, not files, so
links/orphans/banks cannot differ per vendor, and printing them beside a
filtered structure check would imply they had been checked that way.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
```

---

## Task 2: #90 — 실습 예제를 세 벤더 모두에게 돌려준다

**Files:**
- Modify: `tests/test_check_content.py:336-338` (`ShippedContentTest`에 벤더 가드 추가)
- Modify: `02-intermediate/04-backup-recovery-strategies.md:94-130` (실습 예제 절), `:148-150` (체크리스트)

**Interfaces:**
- Consumes: `check_content.check_structure(root, dbms)` — Task 1이 만든 것.
- Produces: 없음 (본문 변경). 이후 Task는 이 파일이 세 벤더 뷰 모두에서 네 절을 갖는다고 가정한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다 — 저장소 본문에 대고**

`tests/test_check_content.py`의 `ShippedContentTest`, `test_chapters_follow_the_four_section_shape`(336-338행) **바로 뒤**에 추가한다.

```python
    def test_each_vendor_view_keeps_the_four_section_shape(self):
        """단일 벤더 브랜치를 잘라 봐야만 드러나는 결함을 여기서 잡는다.

        `main`에서 도는 것이 요점이다 — CI 트리거가 `push: branches: [main]`
        이라 벤더 브랜치에서는 이 스위트가 아예 돌지 않는다. 이슈 #90이
        그 사각지대에서 v1.4.0까지 살아남았다.
        """
        self._skip_on_a_vendor_branch()
        for dbms in ("postgresql", "mysql", "oracle"):
            with self.subTest(dbms=dbms):
                self.assertEqual(
                    check_content.check_structure(REPO_ROOT, dbms), [])
```

- [ ] **Step 2: 실패를 확인한다 — 어느 벤더가 왜 빨간지까지 본다**

Run: `python3 -m unittest tests.test_check_content.ShippedContentTest -v`

Expected: FAIL. `subTest(dbms='mysql')`과 `subTest(dbms='oracle')` 두 개가 실패하고, 실패 메시지에 `02-intermediate/04-backup-recovery-strategies.md: \`## 실습 예제\` 절이 없거나 순서가 어긋난다`가 있다. `postgresql`은 통과한다.

이 세 결과의 조합이 중요하다 — 셋 다 빨갛다면 필터가 아니라 검사기가 고장 난 것이다.

- [ ] **Step 3: 실습 예제 절을 재배치한다**

`02-intermediate/04-backup-recovery-strategies.md`의 **94행부터 130행까지**(`<!-- dbms:postgresql -->`부터 `<!-- /dbms:postgresql -->`까지, 두 마커 줄 포함)를 아래로 통째로 교체한다. 132행의 `## 백업 주기/보관 정책 설계`부터는 손대지 않는다.

````markdown
## 실습 예제 — 특정 시점 복구(PITR)

시나리오: 상시 전체 백업을 받고 그 사이의 변경은 연속 로그로 아카이빙하는 운영 중, 오후 2시 30분에 실수로 전체 `DELETE`가 발생했다. 2시 29분 상태로 되돌린다.

세 DBMS의 원리는 같다 — **목표 시각 이전의 백업을 복원한 뒤, 그 시점부터 목표 시각까지의 로그만 재적용한다.** 다른 것은 로그의 이름, 재적용을 멈추는 방법, 그리고 복구를 끝내고 서비스를 여는 절차다.

<!-- dbms:postgresql -->
### PostgreSQL — base backup + WAL 재생

```bash
# 사전 준비(평상시): 아카이빙이 켜져 있고 base backup이 있는 상태
pg_basebackup -D /backup/base_20260715 -Fp -Xs -P

# --- 사고 발생 후 복구 절차 ---
# 1) DB 정지
pg_ctl stop -D $PGDATA

# 2) 손상된 데이터 디렉터리를 치우고 base backup 복원
mv $PGDATA ${PGDATA}.broken
cp -a /backup/base_20260715 $PGDATA

# 3) 복구 목표 시점 지정 (postgresql.conf 또는 별도 파일)
cat >> $PGDATA/postgresql.conf <<'EOF'
restore_command = 'cp /arch/%f %p'
recovery_target_time = '2026-07-15 14:29:00+09'
recovery_target_action = 'promote'
EOF
touch $PGDATA/recovery.signal   # PG 12+ : 복구 모드 진입 신호

# 4) 기동 → WAL을 목표 시점까지 재생하고 promote
pg_ctl start -D $PGDATA

# 5) 데이터 확인 후 정상 서비스로 전환
```

`recovery_target_action = 'promote'`이므로 목표 시점에 닿으면 자동으로 읽기·쓰기가 가능한 상태가 된다. 재생 결과를 먼저 눈으로 확인하고 싶으면 `pause`로 두고, 확인 후 `SELECT pg_wal_replay_resume();`으로 마무리한다.
<!-- /dbms:postgresql -->

<!-- dbms:mysql -->
### MySQL — 전체 백업 복원 + binlog 재적용

```bash
# 사전 준비(평상시): binlog가 켜져 있고 전체 백업이 있는 상태
#   my.cnf:  log_bin = /var/log/mysql/binlog

# --- 사고 발생 후 복구 절차 ---
# 1) 가장 가까운 전체 백업 복원
mysql < /backup/full_20260715.sql        # 논리 백업인 경우
# (XtraBackup이면 --prepare 후 데이터 디렉터리를 교체한다)

# 2) 재적용을 시작할 binlog 좌표 확인
head -30 /backup/full_20260715.sql       # --master-data가 남긴 CHANGE MASTER 주석
mysql -e "SHOW BINARY LOGS;"             # 그 이후 파일들의 목록

# 3) 백업 시점 이후부터 목표 시각 직전까지만 재적용
mysqlbinlog --start-position=194 \
            --stop-datetime="2026-07-15 14:29:00" \
            /var/log/mysql/binlog.000123 /var/log/mysql/binlog.000124 \
  | mysql

# 4) 데이터 확인 후 정상 서비스로 전환
```

여러 binlog 파일은 **한 번의 `mysqlbinlog` 호출에 함께 넘긴다.** 파일마다 따로 돌려 파이프하면 파일 경계를 걸친 트랜잭션이 끊긴다. GTID가 켜진 인스턴스에 재적용할 때 이미 실행된 트랜잭션이라는 오류가 나면 `--skip-gtids`를 준다.
<!-- /dbms:mysql -->

<!-- dbms:oracle -->
### Oracle — RMAN 불완전 복구

```sql
-- 사전 준비(평상시): ARCHIVELOG 모드 + BACKUP DATABASE PLUS ARCHIVELOG

-- --- 사고 발생 후 복구 절차 ---
-- 1) 데이터 파일을 열지 않은 MOUNT 상태로 기동
RMAN> SHUTDOWN IMMEDIATE;
RMAN> STARTUP MOUNT;

-- 2) 목표 시각 이전의 백업을 복원하고, 아카이브 로그를 그 시각까지만 적용
RMAN> RUN {
  SET UNTIL TIME "TO_DATE('2026-07-15 14:29:00','YYYY-MM-DD HH24:MI:SS')";
  RESTORE DATABASE;
  RECOVER DATABASE;
}

-- 3) 로그 순번을 초기화하며 개방
RMAN> ALTER DATABASE OPEN RESETLOGS;
```

`OPEN RESETLOGS`는 로그 순번을 1부터 다시 매기고 새 인카네이션(incarnation)을 만든다. 10g 이후 RMAN은 인카네이션을 인식하므로 이전 백업으로도 (`RESET DATABASE TO INCARNATION`을 거쳐) 복구할 수 있지만 절차가 눈에 띄게 복잡해지고 실수 여지가 커진다 — **개방 직후 전체 백업을 새로 받는 것까지가 절차다.**
<!-- /dbms:oracle -->

세 절차 모두 검증하는 것은 "백업이 있다"가 아니라 **"백업으로 목표 시각까지 실제로 되돌려 봤다"** 이다. 아카이브 한 조각이 비어 있으면 재적용은 정확히 그 지점에서 멈추는데, 사고 당일에 알게 되면 이미 늦다. 아래 정책이 분기 1회 복구 훈련을 요구하는 이유가 이것이다.
````

**주의:** 위 블록에는 중첩 코드 펜스가 없다. 본문 파일에는 ` ``` `(백틱 3개)만 들어간다 — 이 계획 문서에서 4개로 감싼 것은 계획 자체의 렌더링 때문이다.

- [ ] **Step 4: 체크리스트에 벤더 쌍을 맞춘다**

같은 파일의 148-150행(현행 PostgreSQL 전용 항목)을 교체한다.

```markdown
<!-- dbms:postgresql -->
- [ ] PostgreSQL에서 `recovery_target_time`으로 특정 시점까지 PITR 복구 절차를 수행할 수 있다.
<!-- /dbms:postgresql -->
<!-- dbms:mysql -->
- [ ] MySQL에서 전체 백업을 복원한 뒤 binlog를 목표 시각까지 재적용할 수 있다.
<!-- /dbms:mysql -->
<!-- dbms:oracle -->
- [ ] Oracle에서 RMAN 불완전 복구를 수행하고, `OPEN RESETLOGS` 이후 전체 백업을 새로 받는 이유를 설명할 수 있다.
<!-- /dbms:oracle -->
```

- [ ] **Step 5: 네 뷰를 모두 눈으로 확인한다**

Run:
```bash
for v in postgresql mysql oracle; do
  echo "=== $v ==="
  python3 scripts/filter_dbms.py "$v" /dev/stdout \
    < 02-intermediate/04-backup-recovery-strategies.md 2>/dev/null \
    || python3 -c "
import sys; sys.path.insert(0,'scripts')
import filter_dbms
lines = open('02-intermediate/04-backup-recovery-strategies.md', encoding='utf-8').readlines()
out = filter_dbms.filter_lines(lines, '$v')
print(''.join(l for l in out if l.startswith('#')))"
done
```

Expected: 세 벤더 모두 `## 핵심 개념 설명` → `## 주요 명령어/문법` → `## 실습 예제 — 특정 시점 복구(PITR)` → `## 백업 주기/보관 정책 설계` → `## 체크리스트` 순으로 나오고, `###` 하위 절은 자기 벤더 것 하나만 남는다.

`filter_dbms.py`의 `main()`은 **파일을 제자리에서 덮어쓴다.** 본문에 대고 직접 돌리지 말 것 — 위 명령의 두 번째 갈래(`python3 -c`)가 안전한 형태이고, 첫 갈래가 실패하는 것이 정상이다.

- [ ] **Step 6: 가드가 초록으로 바뀌는지 확인한다**

Run: `python3 -m unittest tests.test_check_content.ShippedContentTest -v`

Expected: PASS. Step 2에서 빨갛던 `subTest(dbms='mysql')`·`subTest(dbms='oracle')`가 초록이 된다.

Run: `python3 scripts/check_content.py --dbms mysql && python3 scripts/check_content.py --dbms oracle`

Expected: 둘 다 `[structure:mysql] ok` / `[structure:oracle] ok`, exit 0.

- [ ] **Step 7: 가드가 진짜 이 결함을 잡는지 되돌려 확인한다 (mutate-and-watch-red)**

```bash
git stash push 02-intermediate/04-backup-recovery-strategies.md
python3 -m unittest tests.test_check_content.ShippedContentTest 2>&1 | tail -5
git stash pop
python3 -m unittest tests.test_check_content.ShippedContentTest 2>&1 | tail -3
```

Expected: stash 상태에서 FAIL(mysql·oracle), pop 후 OK. **이 확인 없이는 가드가 무엇을 잡는지 아무것도 증명되지 않았다** — `ClientCheckSymmetryTest`가 몇 달간 그 상태였다.

- [ ] **Step 8: 전체 스위트**

Run: `python3 -m unittest discover -s tests`

Expected: OK, 891개 (Task 1의 890 + `ShippedContentTest`의 새 메서드 1개. `subTest`는 테스트 수를 늘리지 않는다).

- [ ] **Step 9: 커밋**

```bash
git add tests/test_check_content.py 02-intermediate/04-backup-recovery-strategies.md
git commit -F - <<'MSG'
Give the backup chapter's walkthrough back to mysql and oracle

Fixes #90. The 실습 예제 section sat entirely inside a dbms:postgresql
block, so the mysql and oracle views of this chapter went from commands
straight to the checklist with nothing to actually do — the one thing a
single-vendor branch exists to prevent.

The block held more than the issue recorded: the paragraph explaining
MySQL's PITR and its mysqlbinlog example were inside it too, which made
them MySQL guidance visible only to PostgreSQL readers.

The scenario and the closing paragraph now sit outside the markers and
each vendor gets a complete procedure inside its own — not shared steps
with the commands swapped, because the step structure genuinely differs:
recovery.signal plus promote, binlog coordinates, and OPEN RESETLOGS are
not one procedure with three vocabularies. The checklist gains the mysql
and oracle twins of the PostgreSQL-only line.

ShippedContentTest now runs the structure check through all three vendor
views. Verified by stashing the chapter fix and watching mysql and oracle
go red while postgresql stayed green — all three red would have meant the
checker was broken, not the content.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
```

---

## Task 3: 딸림 점검과 문서

**Files:**
- Modify: `02-intermediate/09-commands-cheatsheet.md:62-66` (「백업 / 복구」 표)
- Modify: `appendix/glossary.md` (`## A-Z` 목록 두 자리)
- Modify: `docs/release-policy.md` (「릴리스 전 점검」 목록)
- Modify: `CLAUDE.md` (`scripts/check_content.py` 설명 문단)
- Verify only: `exams/02-intermediate/04-backup-recovery-strategies.json`

**Interfaces:**
- Consumes: Task 2가 본문에 새로 들인 명령 — `SHOW BINARY LOGS`, `ALTER DATABASE OPEN RESETLOGS`, `RESTORE DATABASE`, `SET UNTIL TIME`.
- Produces: 없음.

- [ ] **Step 1: 문제은행이 새 본문과 어긋나지 않는지 확인한다 (변경 없음이 기대값)**

Run:
```bash
python3 -c "
import json
d = json.load(open('exams/02-intermediate/04-backup-recovery-strategies.json'))
for q in d['questions']:
    if q.get('dbms') != 'neutral':
        print(q['id'], '|', q['dbms'], '|', q['q'][:60])"
```

Expected — 세 문항 모두 **「주요 명령어/문법」 절**을 겨냥한다:
```
bk-pg-dump-custom     | postgresql | PostgreSQL에서 mydb 데이터베이스를 커스텀 포맷으로 …
bk-mysql-dump-consistent | mysql   | MySQL에서 InnoDB 테이블을 잠그지 않고 일관된 스냅샷으로 …
bk-oracle-rman-archivelog | oracle | Oracle RMAN에서 데이터베이스와 아카이브 로그를 함께 …
```

세 문항 중 어느 것도 실습 예제 절을 인용하지 않으므로 **은행은 고치지 않는다.** 벤더 균형도 이미 postgresql 1 / mysql 1 / oracle 1로 맞다. 이 단계의 산출물은 "확인했고 변경 없음"이라는 사실 자체다 — 릴리스 정책이 사람 몫으로 남겨 둔 항목이라 기록해 둔다.

- [ ] **Step 2: 치트시트에 행을 하나 더한다**

`02-intermediate/09-commands-cheatsheet.md`의 「백업 / 복구」 표에서 `| 연속 로그 | …` 행(65행) **바로 아래**에 넣는다.

```markdown
| 연속 로그 목록 확인 | `SELECT * FROM pg_ls_waldir();` | `SHOW BINARY LOGS;` | `SELECT * FROM v$archived_log;` |
```

**이 행 하나만 더하는 이유:** 새로 들인 명령 중 세 벤더에 모두 진짜 대응물이 있는 것은 이것뿐이다. `ALTER DATABASE OPEN RESETLOGS`는 MySQL에 대응물이 없고(binlog 재적용은 그냥 끝난다), 이 표의 77개 행 전부가 세 칸을 채우고 있어 "해당 없음" 칸을 처음 만드는 것은 이 작업이 낼 결정이 아니다. 그 개념은 Step 3의 용어집이 받는다. `RESTORE DATABASE`/`SET UNTIL TIME`은 이미 있는 `PITR 목표 지정` 행(66행)이 대표한다.

- [ ] **Step 3: 용어집에 두 항목을 더한다**

`appendix/glossary.md`의 `## A-Z` 목록은 **대소문자를 무시한 알파벳순 → 가나다순**이다(`Read Replica`, `Reconcile Loop`, `RPO`, `RTO` 순이 그 증거 — ASCII 순이었다면 `RPO`가 앞이다).

`Reconcile Loop` 다음, `RPO` 앞에:

```markdown
- **RESETLOGS** — Oracle에서 불완전 복구를 마치고 데이터베이스를 열 때 로그 순번을 1부터 다시 매기며 새 인카네이션(incarnation)을 만드는 옵션(`ALTER DATABASE OPEN RESETLOGS`). 10g 이후 RMAN은 인카네이션을 인식하므로 이전 백업으로도 (`RESET DATABASE TO INCARNATION`을 거쳐) 복구할 수 있지만 절차가 복잡해지므로, 개방 직후 전체 백업을 새로 받는다. → `02-intermediate/04-backup-recovery-strategies.md`
```

`보안 그룹(Security Group)` 다음, `서브넷 그룹(Subnet Group)` 앞에:

```markdown
- **불완전 복구(Incomplete Recovery)** — 사용 가능한 로그를 끝까지 적용하지 않고 특정 시점에서 멈추는 복구. PITR이 이에 해당하며, 그 시점 이후의 변경은 의도적으로 버린다. → `02-intermediate/04-backup-recovery-strategies.md`
```

- [ ] **Step 4: 릴리스 점검 목록에 `--dbms`를 넣는다**

`docs/release-policy.md`의 「릴리스 전 점검」에서 `- [ ] 챕터를 추가·개정했다면 네 절 구조가 유지된다 — **CI 자동** (structure)` 줄 **바로 아래**에 추가한다.

```markdown
- [ ] 벤더 마커를 건드렸다면 세 단일 벤더 뷰에서도 네 절 구조가 유지된다 — **CI 자동** (`ShippedContentTest`). 손으로 보려면 `python3 scripts/check_content.py --dbms {postgresql,mysql,oracle}`
```

같은 문서의 「CI가 하는 일과 하지 않는 일」 절, `검사기는 **판정 가능한 것만** 본다`로 시작하는 문단 **앞**에 한 문단을 넣는다.

```markdown
구조 검사는 `main`의 트리뿐 아니라 **세 단일 벤더 뷰에도** 돌아간다. 절 하나가 통째로 한 벤더의 마커 안에 들어가면 나머지 두 브랜치에서 그 절이 사라지는데, `main`에서만 보면 정상으로 보인다 — 이슈 #90이 그 모양으로 v1.4.0까지 살아남았다. 필터는 `generate-branch.sh`가 쓰는 `filter_dbms.filter_lines`와 같은 함수라, 검사와 실제 브랜치가 갈라질 수 없다.
```

- [ ] **Step 5: `CLAUDE.md`를 갱신한다**

`scripts/check_content.py` 항목에서 검사 넷을 나열한 문장 —

> `scripts/check_content.py` — the mechanically-decidable half of the release checklist: relative links resolve, no tier/appendix document is missing from `README.md`, every chapter carries the four sections **in order**, every chapter has an `exams/<tier>/<stem>.json`.

의 `in order` 뒤에 이어 붙인다.

> The section check also runs against the **three single-vendor views** (`--dbms`, or the `ShippedContentTest` assertion that covers all three): a section wholly inside one vendor's markers disappears from the other two branches while `main` still looks correct — that is how #90 survived to v1.4.0. The filter is `filter_dbms.filter_lines`, the same function `generate-branch.sh` uses, so the check cannot drift from the branches it stands for.

같은 항목의 마지막 문장 —

> `ShippedContentTest` skips the structure assertion on vendor branches: a section wholly inside one vendor's markers vanishes from the other two views, so the shape is only guaranteed on `main`.

은 **그대로 둔다** (여전히 사실이다). 다만 뒤에 한 문장 더한다.

> That skip is not a blind spot: the vendor-view assertions run on `main`, which is the only branch CI triggers on anyway.

- [ ] **Step 6: 검사기와 스위트를 돌린다**

Run:
```bash
python3 scripts/check_content.py
for v in postgresql mysql oracle; do python3 scripts/check_content.py --dbms "$v"; done
python3 -m unittest discover -s tests
```

Expected: 검사기 네 번 모두 exit 0. 스위트 OK 891개.

`README.md`를 건드리지 않았으므로 `[orphans]`는 그대로 통과한다 — 치트시트·용어집·정책 문서는 모두 기존 파일이다.

- [ ] **Step 7: 커밋**

```bash
git add 02-intermediate/09-commands-cheatsheet.md appendix/glossary.md docs/release-policy.md CLAUDE.md
git commit -F - <<'MSG'
Carry the PITR walkthrough into the cheatsheet, glossary, and policy

The release checklist leaves three judgments to a person: does a new
command belong in the tier cheatsheet, does a new term belong in the
glossary, and is the question bank still true. Recording all three.

Cheatsheet gains one row — continuous-log listing (pg_ls_waldir /
SHOW BINARY LOGS / v$archived_log) — and only that one. OPEN RESETLOGS
has no MySQL counterpart, and all 77 existing rows fill every column, so
inventing the first empty cell is not this change's call to make; the
glossary takes that concept instead. RESTORE DATABASE and SET UNTIL TIME
are already represented by the PITR target row.

Glossary gains RESETLOGS and 불완전 복구, placed by the file's actual
ordering (case-insensitive alphabetical, then 가나다).

The question bank needed no edit: all three vendor questions target the
주요 명령어/문법 section, not the walkthrough, and the vendor balance was
already 1/1/1. Checked, unchanged, recorded here so the next release does
not re-derive it.

release-policy.md and CLAUDE.md now describe the vendor-view structure
check and say why the vendor-branch skip beside it is not a blind spot.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
```

---

## Task 4: 릴리스 노트 초안과 최종 검증

**Files:**
- Create: `docs/superpowers/notes/2026-08-12-v1.5.0-release-notes.md`

**Interfaces:**
- Consumes: Task 1-3의 결과, `main`의 26커밋.
- Produces: `gh release create --notes-file`에 그대로 넘길 파일.

이 파일은 `docs/superpowers/notes/`에 둔다 — `LINKED_DIRS`가 `TIERS + ("appendix",)`이므로 `README.md` 등재가 필요 없고, 릴리스 본문이 정본이라 저장소에 남는 것은 초안일 뿐이다. **`notes/` 디렉터리는 아직 없다** (현재 `docs/superpowers/`에는 `plans/`와 `specs/`뿐) — 이 Task가 처음 만든다.

- [ ] **Step 1: 노트에 들어갈 사실을 다시 재 본다**

Run:
```bash
git log v1.4.0..HEAD --oneline --no-decorate | wc -l
git diff v1.4.0..HEAD --stat | tail -3
python3 -m unittest discover -s tests 2>&1 | tail -3
```

Expected: 커밋 수(설계 문서·Task 1-3 커밋이 더해져 26보다 크다), 변경 통계, `OK` 891개. 노트의 숫자 요약은 **이 출력에서 옮겨 적는다** — 기억이나 추정으로 쓰지 않는다.

- [ ] **Step 2: 노트를 쓴다**

`docs/superpowers/notes/2026-08-12-v1.5.0-release-notes.md`. 아래 골격을 채운다. 절 순서는 `docs/release-policy.md`의 「릴리스 노트 작성」이 정한 기능 축이다.

```markdown
`v1.4.0`이 챕터 본문을 터미널용으로 조판하기 시작했다. 이번 릴리스는 그 조판이
**창 크기를 바꿔도 무너지지 않게** 만들고, 화면 어디서든 한 타로 빠져나올 길을
내고, 다 읽은 챕터에서 시험으로 넘어가는 길을 막지 않는 형태로 바꾼다.

## 챕터 읽기 — 창을 좁혀도 무너지지 않는다

(코드 펜스 상자 제거: 상자가 렌더 폭에서 하드랩되어 명령을 토큰 중간에서 잘랐다 —
80칸에서 코드 줄의 6%, 60칸 25%, 40칸 49%. 화면에서 복사한 명령이 실행되지 않았다.
이제 짧은 언어 라벨 + 4칸 들여쓰기로 내보내고 코드는 그대로 둔다. 접는 것은 less의 몫.)

(표: 자연 폭이 맞을 때만 정렬해 그리고, 넘치면 행마다 카드로 — 44개 표 중 36개가
80칸에서 이미 뭉개지고 있었고 40칸에서는 `PostgreSQL`이 `PostgreSQ`/`L`로 갈라졌다.)

(제목은 자르지 않고 줄바꿈하며 스타일을 이어 간다. 가로선은 24칸에서 멈춘다.)

## 어디서든 `Q` (#95)

(`Esc`/`q`는 한 단계 뒤로, 대문자 `Q`는 앱 종료. 챕터마다 묻던 "계속하려면 Enter"는
없앴다. 예외 둘: 게임 진행 중 서버 선택 화면, 그리고 exam 화면.)

## 읽고 나서 바로 풀기 (#96)

(챕터 목록에서 `x`. 목록 라벨에 `[시험 없음]`·`[지난 최고 A·92%]`.
읽고 나올 때마다 묻던 `[Y/n]` 프롬프트는 없앴다 — 기본값이 예라서
습관적인 Enter가 시험을 시작시켰다.)

## 학습서 — mysql·oracle 독자에게 실습을 돌려준다 (#90)

(백업·복구 챕터의 실습 예제가 통째로 postgresql 마커 안에 있었다. 이제 시나리오는
공유하고 세 벤더가 각자의 완전한 절차를 갖는다. 같은 부류를 다시 들이지 않도록
구조 검사가 세 벤더 뷰에도 돌아간다 — `check_content.py --dbms`.)

## 알려진 한계

- East Asian Ambiguous 글리프(`─ │ ━ • · ◆`)를 2칸으로 그리도록 설정한 터미널에서는
  256개 렌더 줄이 80칸을 넘는다(이번 작업 전 853줄). `tui.cwidth`의 저장소 전역
  가정이고 이식 가능한 탐지 수단이 없다.
- **학습 점검(`exam`) 화면은 아직 자체 키 계약을 쓴다** — `q`는 시험만 끝내고
  `Esc`는 반응하지 않는다. `Q`가 닿지 않는 유일한 화면이다.
- 장애 대응 게임이 **진행 중**일 때 서버 선택 화면은 `Q`를 막아 뒀다 — 게임을 끊으면
  `dbshoot-*` 컨테이너가 뜬 채 남는다. 그 화면은 `Esc`로 취소한다.
- PostgreSQL 판정의 구멍 둘 — 파싱 오류는 감시에 남지 않고, pid를 적지 않는
  `pg_terminate_backend` 쓸기는 `kill_precision`이 잡지 못한다(`forbidden_command`로 막는다).
- 장애 대응 스테이지의 벤더 분포는 MySQL 13 / PostgreSQL 1 / Oracle 0이다 (#79).
- **Oracle RMAN 절차는 실행 검증이 아니라 문서 근거로 작성됐다** — 실습 랩에는
  MySQL과 PostgreSQL만 있다.
- 치트시트 행·용어집 반영·문제은행 내용의 최신성은 여전히 사람 몫이다.

---

(숫자 요약: 커밋 N개, 파일 N개, +N/-N. Step 1의 출력에서 옮긴다.)

전체 변경 이력: https://github.com/Ahngbeom/dba-guide/commits/v1.5.0
```

괄호 안 지시는 실제 문장으로 풀어 쓰고 지운다. **괄호가 남은 채 커밋하지 않는다.**

- [ ] **Step 3: 최종 검증 — 릴리스 정책의 자동 항목을 전부 돌린다**

Run:
```bash
git status --short
python3 -m unittest discover -s tests
python3 scripts/check_content.py
for v in postgresql mysql oracle; do python3 scripts/check_content.py --dbms "$v"; done
```

Expected: `git status`는 노트 파일만(커밋 전), 스위트 OK 890개, 검사기 네 번 모두 exit 0.

- [ ] **Step 4: `./shoot doctor` — 사람 몫**

Run: `./shoot doctor`

docker와 mysql/psql 클라이언트가 필요하다. 에이전트는 이 단계를 대신 통과시킬 수 없다. 결과를 사람에게 확인받고, 실패하면 그 원인이 이 계획의 변경과 무관함을 확인한 뒤 진행한다(이 계획은 `shooting/`을 건드리지 않는다).

- [ ] **Step 5: 커밋**

```bash
git add docs/superpowers/notes/2026-08-12-v1.5.0-release-notes.md
git commit -F - <<'MSG'
Draft the v1.5.0 release notes

Organized by what a reader can now do rather than by commit order, per
docs/release-policy.md: reading that survives a narrow window, one key
out of any screen, the chapter-list handoff into an exam, and the vendor
views getting their walkthrough back.

The 알려진 한계 section names the exam screen's separate key contract,
the ambiguous-width limit, the two PostgreSQL judging holes, the stage
vendor skew, and the fact that the Oracle RMAN procedure was written from
documentation rather than a run — the lab has no Oracle. Stating these
now makes them agreed boundaries instead of bug reports later.

Kept out of README.md and the tier directories on purpose: the published
release body is the canonical history, and this file is only the draft
handed to `gh release create --notes-file`.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
```

---

## Task 5: 릴리스 실행

TDD 사이클이 아니라 순서가 고정된 절차다. 각 단계의 **주체**를 지킨다 — 사람 몫으로 표시된 것을 에이전트가 대신하지 않는다.

**Files:** 없음 (git·gh 조작)

- [ ] **Step 1: PR을 올린다** — 사람 확인 후

```bash
git push -u origin Ahngbeom/issue-90
gh pr create --base main --title "v1.5.0 준비 — #90과 벤더 뷰 구조 검사" \
  --body-file docs/superpowers/specs/2026-08-12-v1.5.0-release-design.md
```

- [ ] **Step 2: CI 초록 확인 후 머지** — 사람

```bash
gh pr checks --watch
```

머지 후 `git switch main && git pull`.

- [ ] **Step 3: 벤더 브랜치 3개 재생성 — 되돌릴 수 없다, 사람 확인 후**

현재 셋 다 `main@e86310d`(= v1.4.0) 기준이다.

```bash
for v in postgresql mysql oracle; do
  ./scripts/generate-branch.sh "$v"
  git push --force-with-lease origin "$v"
done
```

`generate-branch.sh`는 `git worktree add -B "$target" main`으로 매번 벤더 브랜치를 `main`으로 리셋하므로, 결과는 항상 "main + 커밋 1개"다. 그래서 정책이 `--force-with-lease`를 요구한다. **원격 히스토리를 덮어쓰므로 실행 전 사람에게 확인받는다.**

- [ ] **Step 4: 재생성 결과를 검사한다**

```bash
for v in postgresql mysql oracle; do
  git switch --detach "origin/$v"
  python3 -m unittest tests.test_check_content -v 2>&1 | tail -3
done
git switch main
```

Expected: 세 브랜치 모두 OK. `ShippedContentTest`의 구조 검사 둘은 `_skip_on_a_vendor_branch`로 건너뛴다(마커가 이미 필터되어 없으므로) — `skipped` 표시가 나오는 것이 정상이다.

- [ ] **Step 5: 초안 릴리스를 만든다 — 태그는 아직 생기지 않는다**

```bash
gh release create v1.5.0 --draft --target "$(git rev-parse main)" \
  --title "v1.5.0 — 좁은 창에서도 읽히고, 어디서든 나갈 수 있고, 세 벤더가 같이 실습한다" \
  --notes-file docs/superpowers/notes/2026-08-12-v1.5.0-release-notes.md
```

`--target`에 브랜치 이름이 아니라 `git rev-parse main`의 결과를 넣는다 — 초안이 떠 있는 동안 `main`에 커밋이 쌓이면, 브랜치 이름으로 잡아 둔 초안은 노트를 쓸 때 보던 트리가 아닌 곳에 태그를 찍는다.

- [ ] **Step 6: 웹에서 렌더링을 확인하고 발행 — 사람**

```bash
gh release view v1.5.0 --web
gh release edit v1.5.0 --draft=false     # 태그는 이때 생긴다
```

- [ ] **Step 7: 인스톨러 실검증 — 사람 몫**

```bash
curl -fsSL https://raw.githubusercontent.com/Ahngbeom/dba-guide/main/install.sh | bash
```

`install.sh`는 **최신 정식 태그**를 설치한다. CI는 저장소 안에서 스크립트를 실행할 뿐 발행된 태그를 집어 오지 않으므로, 새 태그를 실제로 집는지는 여기서만 확인된다. 에이전트가 대신 통과시킬 수 없다.

- [ ] **Step 8: 이슈를 닫는다**

```bash
gh issue close 90 --comment "v1.5.0에서 수정. 실습 예제를 공통 시나리오 + 벤더별 완전 절차로 재배치하고, 같은 부류가 다시 들어오지 않도록 구조 검사를 세 벤더 뷰에 걸었다(check_content.py --dbms, ShippedContentTest)."
```

- [ ] **Step 9: 스테일 원격 브랜치 정리 — 건별 확인 후**

태그가 찍힌 **뒤에** 지운다. `v1.5.0`이 해당 커밋들을 붙잡으므로 도달 불가 커밋이 생기지 않는다.

```bash
for b in Ahngbeom/issue-95 Ahngbeom/issue-96 Ahngbeom/reading-exam-handoff \
         Ahngbeom/shooting-infra Ahngbeom/dbms-by-branch \
         Ahngbeom/kube-oracle-mysql-operator feature/tui-exam Ahngbeom/issue-90; do
  echo "=== $b"; git cherry origin/main "origin/$b" | grep '^+' || echo "  main에 전부 반영됨"
done
```

Expected: 여덟 개 모두 `main에 전부 반영됨`. `+`가 하나라도 찍히는 브랜치는 **지우지 않고 사람에게 보고한다.** 확인 후 건별로 `git push origin --delete <branch>`.

---

## 자체 검토 메모

**설계 문서 대비 커버리지**

| 설계 절 | 담당 Task |
|---|---|
| §1.1-1.2 결함과 범위 측정 | Task 2 Step 2 (실패 확인이 곧 재측정) |
| §1.3-1.4 새 구조와 벤더 3절 | Task 2 Step 3 |
| §1.5 체크리스트 쌍 | Task 2 Step 4 |
| §1.6 딸림 점검 3건 | Task 3 Step 1-3 |
| §2.1 검사기 + `--dbms` | Task 1 Step 3-4 |
| §2.2 스위트 | Task 1 Step 1(픽스처), Task 2 Step 1(저장소 본문) |
| §2.3 mutate-and-watch-red | Task 2 Step 7 |
| §3 릴리스 노트 | Task 4 Step 2 |
| §4 실행 순서 10단계 | Task 5 (1-9) + Task 4 Step 4 (`shoot doctor`) |
| §5 위험 | Task 4 Step 4, Task 5 Step 3·7에 각각 명시 |

**이름 일관성** — `check_structure(root, dbms=None)`가 Task 1에서 정의되고 Task 2 Step 1·Task 5 Step 4에서 같은 이름·같은 인자 순서로 호출된다. CLI 라벨은 세 곳 모두 `structure:<dbms>`다. 픽스처 상수는 `VENDOR_CHAPTER` 하나이고 Task 1 안에서만 쓰인다.

**의도적으로 하지 않는 것**

- `--dbms all` 같은 편의 플래그. 릴리스 점검이 세 번 돌리면 되고, 지금 없는 요구다.
- `check_links`/`check_orphans`/`check_banks`의 벤더화. 필터는 줄을 지우지 절 파일을 지우지 않는다.
- `_skip_on_a_vendor_branch` 제거. Global Constraints에 이유가 있다.
- `scripts/exam.py`·`shooting/` 일체. 이 릴리스는 그 둘의 동작을 바꾸지 않는다.
